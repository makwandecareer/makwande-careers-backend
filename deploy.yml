name: Production Deploy

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Build Docker Image
        run: docker build -t makwande-careers-backend .

      - name: Placeholder Deployment
        run: |
          echo "Configure your cloud deployment (Azure, AWS, DigitalOcean, etc.) here."
